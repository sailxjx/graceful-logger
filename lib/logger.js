// Generated by CoffeeScript 1.6.3
(function() {
  var Logger, colors;

  colors = require('colors');

  Logger = (function() {
    Logger.prototype.format = function() {
      var ft, i, matches, msgs, replaces, v;
      msgs = (function() {
        var _results;
        _results = [];
        for (i in arguments) {
          v = arguments[i];
          _results.push(v);
        }
        return _results;
      }).apply(this, arguments);
      if ((this.options.format != null) && typeof this.options.format === 'string') {
        matches = this.options.format.split(' ');
        replaces = {
          '%s': function(i) {
            i = Number(i);
            return matches = matches.slice(0, +(i - 1) + 1 || 9e9).concat(msgs, matches.slice(i + 1));
          },
          '%t': function(i) {
            return matches[i] = new Date;
          }
        };
        for (i in matches) {
          ft = matches[i];
          if (replaces[ft] != null) {
            replaces[ft].call(this, i);
          }
        }
        msgs = matches;
      }
      return msgs;
    };

    function Logger(options) {
      this.options = options != null ? options : {};
      this.prefix = {
        info: 'info:',
        warn: 'WARN:',
        err: 'ERR!:'
      };
    }

    Logger.prototype.info = function() {
      var args, prefix;
      args = this.format.apply(this, arguments);
      prefix = process.stdout.isTTY ? this.prefix.info.green : this.prefix.info;
      args.unshift(prefix);
      return console.log.apply(this, args);
    };

    Logger.prototype.warn = function() {
      var args, prefix;
      args = this.format.apply(this, arguments);
      prefix = process.stdout.isTTY ? this.prefix.warn.yellow : this.prefix.warn;
      args.unshift(prefix);
      return console.warn.apply(this, args);
    };

    Logger.prototype.err = function() {
      var args, prefix;
      args = this.format.apply(this, arguments);
      prefix = process.stdout.isTTY ? this.prefix.err.red : this.prefix.err;
      args.unshift(prefix);
      return console.error.apply(this, args);
    };

    return Logger;

  })();

  module.exports = Logger;

}).call(this);
